import 'package:flutter/material.dart';

class EstadosFragment extends StatelessWidget {
  const EstadosFragment({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Estados',
        style: TextStyle(fontSize: 35),
      ),
    );
  }
}
